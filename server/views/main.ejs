<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/x-icon" href="/server/public/favicon.ico">
    <title>Code Insight</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        body {
            background-color: #f8f9fa;
            font-family: "Segoe UI", Roboto, sans-serif;
        }
        .main-content {
            flex-grow: 1; /* Allows this content to take up available space */
        }
        .report-card {
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }
        .report-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        h1.page-title {
            font-size: 2.25rem;
            font-weight: 600;
            color: #343a40;
        }
        /* Floating Action Button (FAB) for "Add New" */
        .fab {
            position: fixed;
            bottom: 40px;
            right: 40px;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            font-size: 1.5rem; /* Makes the icon larger */
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        footer {
            text-align: center;
            padding: 1.5rem;
            color: #6c757d;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>

    <div class="d-flex flex-column min-vh-100">

        <%- include('partials/header.ejs') %>

        <main class="container main-content my-5">
            <h1 class="page-title mb-4">Your Reports</h1>
            
            <div class="row">
                <% if (reports && reports.length > 0) { %>
                    <% reports.forEach(report => { %>
                        <%- include('partials/reportbox.ejs', { report: report }); %>
                    <% }); %>
                <% } else { %>
                    <div class="col-12">
                        <p class="text-muted">You have no reports yet. Click the '+' button to upload a file and generate your first report.</p>
                    </div>
                <% } %>
            </div>
            
        </main>

        <button type="button" class="btn btn-primary fab" data-bs-toggle="modal" data-bs-target="#uploadModal">
            <i class="bi bi-plus-lg"></i>
        </button>

        <%- include('partials/footer.ejs') %> 
    </div>

    <div class="modal fade" id="uploadModal" tabindex="-1" aria-labelledby="uploadModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="uploadModalLabel">Generate New Report</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form action="/upload" method="post" enctype="multipart/form-data" id="uploadForm">
                    <div class="modal-body">
                        <p class="text-muted mb-3">Select a code file or a compressed folder (.zip, .tar) to begin the analysis.</p>
                        
                        <div class="mb-3">
                            <label for="projectNameInput" class="form-label">Project Name</label>
                            <input class="form-control" type="text" name="projectName" id="projectNameInput" required>
                        </div>

                        <div class="mb-3">
                            <label for="fileInput" class="form-label">File to Upload</label>
                            <input class="form-control" type="file" name="testfile" id="fileInput" required>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button class="btn btn-primary" type="submit">Upload & Analyze</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>