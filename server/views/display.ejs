<!DOCTYPE html>
<html>
<head>
    <title>Code Analysis Report</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            background-color: #f8f9fa;
            color: #333;
        }

        .header {
            background-color: #fff;
            padding: 15px 25px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header h1 {
            font-size: 22px;
            margin: 0;
            color: #2c3e50;
        }

        .btn-back {
            background-color: #007bff;
            color: #fff;
            padding: 8px 14px;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            font-size: 14px;
            transition: background-color 0.2s ease;
        }

        .btn-back:hover {
            background-color: #0056b3;
        }

        .container {
            max-width: 800px;
            margin: 30px auto;
            background: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 1px 5px rgba(0,0,0,0.08);
        }

        h2 {
            border-bottom: 1px solid #e1e1e1;
            padding-bottom: 6px;
            margin-top: 25px;
            color: #34495e;
            font-size: 18px;
        }

        h3 {
            font-size: 15px;
            margin-bottom: 5px;
            color: #2c3e50;
        }

        .summary {
            background-color: #f0f4f8;
            padding: 15px 20px;
            border-left: 4px solid #007bff;
            border-radius: 4px;
            margin-bottom: 20px;
        }

        .section {
            margin-bottom: 20px;
        }

        pre {
            background-color: #f5f5f5;
            color: #212529;
            padding: 12px;
            border-radius: 5px;
            font-size: 14px;
            white-space: pre-wrap;      /* ✅ wrap long lines instead of scrolling */
            word-wrap: break-word;      /* ✅ break long words if needed */
            overflow-x: auto;           /* keep safe in case of very long code lines */
        }


        p {
            margin-top: 0;
            font-size: 15px;
        }

        footer {
            text-align: center;
            margin-top: 30px;
            padding: 15px;
            font-size: 13px;
            color: #777;
        }
    </style>
</head>
<body>


    <div class="header">
        <h1>Code Analysis Report</h1>
        <a href="/" class="btn-back">← Back to Dashboard</a>
    </div>

    <div class="container">
        <% if (analysis && analysis.summary) { %>

            <div class="summary">
                <h2>Summary</h2>
                <p><%= analysis.summary %></p>
            </div>

            <div class="section">
                <h2>Best Practices</h2>
                <h3>Comments</h3>
                <p><%= analysis.best_practices.comments %></p>
                <h3>Suggestions</h3>
                <pre><%= analysis.best_practices.suggestions %></pre>
            </div>

            <div class="section">
                <h2>Quality of Code</h2>
                <h3>Comments</h3>
                <p><%= analysis.quality_of_code.comments %></p>
                <h3>Suggestions</h3>
                <pre><%= analysis.quality_of_code.suggestions %></pre>
            </div>

        <% } else { %>
            <p>No analysis data available or the data is in an incorrect format.</p>
        <% } %>
    </div>

    <footer>
        Generated automatically by the Code Analysis System
    </footer>

</body>
</html>
