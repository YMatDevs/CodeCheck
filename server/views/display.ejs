<!DOCTYPE html>
<html>
<head>
  <title>Code Analysis Report</title>
  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      background-color: #f8f9fa;
      color: #333;
    }

    .header {
      background-color: #fff;
      padding: 15px 25px;
      border-bottom: 1px solid #ddd;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header h1 {
      font-size: 22px;
      margin: 0;
      color: #2c3e50;
    }

    .btn-back {
      background-color: #007bff;
      color: #fff;
      padding: 8px 14px;
      border: none;
      border-radius: 4px;
      text-decoration: none;
      font-size: 14px;
      transition: background-color 0.2s ease;
    }

    .btn-back:hover {
      background-color: #0056b3;
    }

    .container {
      max-width: 900px;
      margin: 30px auto;
      background: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 1px 5px rgba(0,0,0,0.08);
    }

    h2 {
      border-bottom: 1px solid #e1e1e1;
      padding-bottom: 6px;
      margin-top: 25px;
      color: #34495e;
      font-size: 18px;
    }

    h3 {
      font-size: 15px;
      margin-bottom: 5px;
      color: #2c3e50;
    }

    .summary {
      background-color: #f0f4f8;
      padding: 15px 20px;
      border-left: 4px solid #007bff;
      border-radius: 4px;
      margin-bottom: 20px;
    }

    .section {
      margin-bottom: 20px;
    }

    .file-section {
      margin-top: 25px;
      padding: 20px;
      border: 1px solid #e1e1e1;
      border-radius: 6px;
      background-color: #fafafa;
    }

    pre {
      background-color: #f5f5f5;
      color: #212529;
      padding: 12px;
      border-radius: 5px;
      font-size: 14px;
      white-space: pre-wrap;
      word-wrap: break-word;
      overflow-x: auto;
    }

    p {
      margin-top: 0;
      font-size: 15px;
    }

    footer {
      text-align: center;
      margin-top: 30px;
      padding: 15px;
      font-size: 13px;
      color: #777;
    }
  </style>
</head>
<body>

  <div class="header">
    <h1>Code Analysis Report</h1>
    <a href="/" class="btn-back">← Back to Dashboard</a>
  </div>

  <div class="container">
    <% if (analysis && analysis.summary) { %>

      <!-- Project Overview -->
      <div class="summary">
        <h2>Project Overview</h2>
        <p><strong>Summary:</strong> <%= analysis.summary %></p>
        <p><strong>Flow:</strong> <%= analysis.flow %></p>
        <p><strong>Project Comments:</strong> <%= analysis.project_comments %></p>
      </div>

      <!-- File-wise Analysis -->
      <div class="section">
        <h2>File Analysis</h2>
        <% if (analysis.files && analysis.files.length > 0) { %>
          <% analysis.files.forEach(file => { %>
            <div class="file-section">
              <h3>📄 <%= file.filename %></h3>
              <p><strong>Summary:</strong> <%= file.summary %></p>
              <p><strong>Comments:</strong></p>
              <pre><%= file.comments %></pre>
              <p><strong>Best Practices & Suggestions:</strong></p>
              <pre><%= file.best_practices %></pre>
            </div>
          <% }) %>
        <% } else { %>
          <p>No file-specific analysis found.</p>
        <% } %>
      </div>

    <% } else { %>
      <p>No analysis data available or the data format is invalid.</p>
    <% } %>
  </div>

  <footer>
  </footer>

</body>
</html>
